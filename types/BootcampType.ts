export type BootcampType = {
    id?: string,
    title?: string,
    description?: string,
    mentor?: string,
    quota?: number,
    location?: string,
    startDate?: Date,
    endDate?: Date,
    banner?: string,
    price?: number,
    registrations?: any
}